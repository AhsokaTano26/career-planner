# career-ai 依赖（与 .venv 保持一致；Python 3.12，对齐 Dockerfile python:3.12-slim）
fastapi>=0.141
uvicorn>=0.52
httpx>=0.28
pydantic>=2.13
python-dotenv>=1.2
python-multipart>=0.0.20
pytest>=8.0
# AI 网关（复用 LiteLLM：多供应商路由/重试/降级/日志回调）
# 注意：升级 litellm 前先查 https://docs.litellm.ai/blog 安全公告（2026-03 曾发生 v1.82.7/v1.82.8 投毒事件）；
# 钉死小版本，升级后重跑 pytest + ai_gateway_smoke 双模式。
# 供应链核验（2026-09-06 已对 PyPI 做 sha256 校验一致）：安装/升级后执行
#   ~/devtools/python/bin/pip hash <wheel文件> 与 PyPI 页 JSON-Hashes 比对，无一致不合入；
# 后续迭代替换位置：切 --require-hashes 锁定全依赖。
litellm==1.98.0
# ai_call_log 落库（MySQL，career-ai 仅写日志表）
sqlalchemy>=2.0
pymysql>=1.1
cryptography>=44.0
