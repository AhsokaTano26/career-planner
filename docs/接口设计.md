# 生涯规划系统 · API 接口文档

> 本文档由 **Apifox 项目（ID 8662286）** 在线 OpenAPI 定义同步生成，**一切内容以 Apifox 线上为准**。
> 同步时间：2026-08-15 ｜ 接口统一前缀：`/api/v1` ｜ 协议：HTTP + JSON
> 来源：Apifox CLI 拉取（`endpoint list` + `export openapi/markdown`，只读，未上传） ｜ 接口总数：**122** ｜ 目录：16 个

---

## 一、接口总览

| HTTP 方法 | 数量 |
|-----------|------|
| GET | 54 |
| POST | 48 |
| PUT | 2 |
| PATCH | 13 |
| DELETE | 5 |

### 认证与账号（9 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/auth/login` | 登录 |
| `POST` | `/api/v1/auth/logout` | 退出登录 |
| `GET` | `/api/v1/auth/me` | 当前用户信息 |
| `PATCH` | `/api/v1/auth/me/password` | 修改密码 |
| `POST` | `/api/v1/auth/password/reset` | 管理员重置密码 |
| `POST` | `/api/v1/auth/privacy-consent` | 同意隐私授权 |
| `GET` | `/api/v1/auth/privacy-consent/status` | 查询授权状态 |
| `POST` | `/api/v1/auth/refresh` | 刷新访问令牌 |
| `POST` | `/api/v1/auth/register` | 学号注册 |

### 学生档案（8 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/students/me` | 获取我的档案 |
| `PATCH` | `/api/v1/students/me` | 分步保存学生资料 |
| `GET` | `/api/v1/students/me/completeness` | 资料完整度明细 |
| `POST` | `/api/v1/students/me/deletion-request` | 申请删除本人信息 |
| `GET` | `/api/v1/students/me/experiences` | 经历列表 |
| `POST` | `/api/v1/students/me/experiences` | 新增经历 |
| `PATCH` | `/api/v1/students/me/experiences/{experienceId}` | 修改经历 |
| `DELETE` | `/api/v1/students/me/experiences/{experienceId}` | 删除经历 |

### 测评（9 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/assessment-sessions` | 创建测评会话 |
| `GET` | `/api/v1/assessment-sessions` | 我的测评会话 |
| `GET` | `/api/v1/assessment-sessions/{sessionId}` | 会话详情 |
| `PUT` | `/api/v1/assessment-sessions/{sessionId}/answers` | 保存 / 自动保存答案 |
| `GET` | `/api/v1/assessment-sessions/{sessionId}/scores` | 得分明细 |
| `POST` | `/api/v1/assessment-sessions/{sessionId}/submit` | 提交并计分 |
| `GET` | `/api/v1/questionnaires` | 问卷列表 |
| `GET` | `/api/v1/questionnaires/{questionnaireId}` | 问卷详情 |
| `GET` | `/api/v1/questionnaires/{questionnaireId}/versions` | 问卷版本历史 |

### 学生画像（5 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/profile-snapshots/{snapshotId}` | 画像快照详情 |
| `POST` | `/api/v1/profile-snapshots/{snapshotId}/feedback` | 画像反馈 |
| `GET` | `/api/v1/students/me/profile/latest` | 最新画像 |
| `POST` | `/api/v1/students/me/profile/refresh` | 重新生成画像 |
| `GET` | `/api/v1/students/me/profile/versions` | 画像版本列表 |

### 路径与方向（7 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/directions` | 方向列表 |
| `GET` | `/api/v1/directions/compare` | 方向对比 |
| `GET` | `/api/v1/directions/{directionId}` | 方向详情 |
| `GET` | `/api/v1/paths` | 发展路径列表 |
| `GET` | `/api/v1/students/me/favorites` | 我的收藏 |
| `POST` | `/api/v1/students/me/favorites/{directionId}` | 收藏方向 |
| `DELETE` | `/api/v1/students/me/favorites/{directionId}` | 取消收藏 |

### 方向推荐（5 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/recommendation-results/{resultId}/feedback` | 推荐反馈 |
| `GET` | `/api/v1/recommendation-runs/{runId}` | 推荐批次详情 |
| `GET` | `/api/v1/students/me/recommendations` | 推荐批次历史 |
| `GET` | `/api/v1/students/me/recommendations/latest` | 最新推荐结果 |
| `POST` | `/api/v1/students/me/recommendations/runs` | 创建推荐批次 |

### 目标计划（14 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/goal-versions` | 目标版本历史 |
| `GET` | `/api/v1/plan-versions` | 计划版本历史 |
| `PATCH` | `/api/v1/plans/{planId}` | 编辑计划 |
| `POST` | `/api/v1/plans/{planId}/confirm` | 确认计划 |
| `GET` | `/api/v1/students/me/goals` | 我的目标 |
| `POST` | `/api/v1/students/me/goals` | 设置 / 变更目标 |
| `POST` | `/api/v1/students/me/plans/draft` | 生成计划草案 |
| `GET` | `/api/v1/students/me/plans/latest` | 最新计划 |
| `GET` | `/api/v1/students/me/reminders` | 站内提醒 |
| `GET` | `/api/v1/tasks` | 任务列表 |
| `POST` | `/api/v1/tasks` | 新增任务 |
| `PATCH` | `/api/v1/tasks/{taskId}` | 更新任务 |
| `DELETE` | `/api/v1/tasks/{taskId}` | 删除任务 |
| `POST` | `/api/v1/tasks/{taskId}/checkin` | 任务打卡 |

### 阶段复盘（8 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/reviews` | 复盘列表 |
| `POST` | `/api/v1/reviews/drafts` | 创建复盘草稿 |
| `GET` | `/api/v1/reviews/{reviewId}` | 复盘详情 |
| `POST` | `/api/v1/reviews/{reviewId}/adopt-advice` | 采纳调整建议 |
| `POST` | `/api/v1/reviews/{reviewId}/ai-summary` | 生成 AI 阶段总结 |
| `PUT` | `/api/v1/reviews/{reviewId}/draft` | 保存复盘草稿 |
| `POST` | `/api/v1/reviews/{reviewId}/guidance-request` | 申请辅导员指导 |
| `POST` | `/api/v1/reviews/{reviewId}/submit` | 提交复盘 |

### AI 智能服务（5 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/ai/chat` | 生涯咨询问答 |
| `POST` | `/api/v1/ai/pdf/parse` | 解析培养方案 PDF |
| `POST` | `/api/v1/ai/plan/generate` | 生成学期计划草案 |
| `POST` | `/api/v1/ai/recommendation/explain` | 生成推荐解释 |
| `POST` | `/api/v1/ai/review/summarize` | 生成阶段总结 |

### AI 生涯咨询（2 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/ai/chat/history` | 会话历史 |
| `POST` | `/api/v1/ai/chat/{messageId}/feedback` | 回答反馈 |

### 辅导员端（7 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/advisor/attention` | 需关注学生 |
| `GET` | `/api/v1/advisor/statistics` | 群体统计 |
| `GET` | `/api/v1/advisor/students` | 所带学生列表 |
| `GET` | `/api/v1/advisor/students/{studentId}` | 学生详情总览 |
| `POST` | `/api/v1/advisor/students/{studentId}/advice` | 提出建议任务 / 建议重新测评 |
| `GET` | `/api/v1/advisor/students/{studentId}/guidance` | 指导记录 |
| `POST` | `/api/v1/advisor/students/{studentId}/guidance` | 填写指导意见 |

### 管理端·用户与白名单（9 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/admin/relations` | 辅导员学生关系 |
| `POST` | `/api/v1/admin/relations` | 批量建立关系 |
| `DELETE` | `/api/v1/admin/relations/{relationId}` | 解除关系 |
| `GET` | `/api/v1/admin/users` | 用户列表 |
| `PATCH` | `/api/v1/admin/users/{userId}` | 更新用户 |
| `GET` | `/api/v1/admin/whitelist` | 白名单列表 |
| `POST` | `/api/v1/admin/whitelist` | 新增白名单 |
| `POST` | `/api/v1/admin/whitelist/import` | 批量导入白名单（CSV） |
| `DELETE` | `/api/v1/admin/whitelist/{whitelistId}` | 删除白名单 |

### 管理端·配置（21 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/admin/abilities` | 能力标签列表 |
| `POST` | `/api/v1/admin/abilities` | 新增能力标签 |
| `PATCH` | `/api/v1/admin/abilities/{tagId}` | 更新能力标签 |
| `GET` | `/api/v1/admin/directions` | 方向库管理 |
| `POST` | `/api/v1/admin/directions` | 新增方向 |
| `PATCH` | `/api/v1/admin/directions/{directionId}` | 更新方向 |
| `PATCH` | `/api/v1/admin/directions/{directionId}/status` | 启停方向 |
| `GET` | `/api/v1/admin/models` | 模型与提示词配置 |
| `POST` | `/api/v1/admin/models/prompts` | 新建提示词版本 |
| `POST` | `/api/v1/admin/models/prompts/{promptVersionId}/publish` | 发布提示词 |
| `PATCH` | `/api/v1/admin/models/{key}` | 更新模型配置 |
| `GET` | `/api/v1/admin/questionnaires` | 问卷管理列表 |
| `POST` | `/api/v1/admin/questionnaires` | 新建问卷 |
| `GET` | `/api/v1/admin/questionnaires/{questionnaireId}/preview` | 问卷预览 |
| `PATCH` | `/api/v1/admin/questionnaires/{questionnaireId}/status` | 发布 / 停用问卷 |
| `POST` | `/api/v1/admin/questionnaires/{questionnaireId}/versions` | 新建问卷版本 |
| `GET` | `/api/v1/admin/templates` | 任务模板列表 |
| `POST` | `/api/v1/admin/templates` | 新增任务模板 |
| `PATCH` | `/api/v1/admin/templates/{templateId}` | 更新任务模板 |
| `GET` | `/api/v1/admin/weights` | 推荐权重配置 |
| `POST` | `/api/v1/admin/weights` | 更新推荐权重 |

### 管理端·培养方案（8 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/admin/curricula/import` | 上传培养方案 PDF |
| `GET` | `/api/v1/admin/curricula/items` | 待审核课程列表 |
| `POST` | `/api/v1/admin/curricula/items/batch` | 批量校核 / 合并 / 删除 |
| `PATCH` | `/api/v1/admin/curricula/items/{itemId}` | 校核课程 |
| `GET` | `/api/v1/admin/curricula/jobs` | 导入任务列表 |
| `GET` | `/api/v1/admin/curricula/jobs/{jobId}` | 导入任务详情 |
| `POST` | `/api/v1/admin/curricula/publish` | 发布培养方案版本 |
| `GET` | `/api/v1/admin/curricula/versions` | 方案版本列表 |

### 管理端·日志与导出（5 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/admin/exports` | 创建导出任务 |
| `GET` | `/api/v1/admin/exports` | 导出任务列表 |
| `GET` | `/api/v1/admin/exports/{jobId}/download` | 下载导出文件 |
| `GET` | `/api/v1/admin/logs/ai` | AI 调用日志 |
| `GET` | `/api/v1/admin/logs/operations` | 操作审计日志 |

---

## 二、重点模块详细定义（推荐 / 画像 / 计划）


### 推荐 Recommendations — `/api/v1/students/me/recommendations`（5 个）

#### 1. 创建推荐批次

```
POST /api/v1/students/me/recommendations/runs
```

触发三段式推荐（规则过滤 → 结构化评分 → AI 解释）。`requestId` 幂等。
AI 不可用时自动降级：仍返回排序结果，仅解释使用规则模板。

**Request Body**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `pathFilter` | string | 发展路径过滤（如 employment） |
| `requestId` | string | 幂等请求 ID |

**响应 200**：推荐批次（可能为 RUNNING，需轮询详情）

| 字段 | 类型 | 说明 |
|------|------|------|
| `runId` | string | 推荐批次 ID |
| `profileVersion` | integer | 画像版本号 |
| `ruleVersion` | string | 规则版本（如 R1.0） |
| `generatedAt` | string | 生成时间 |
| `status` | string | RUNNING / SUCCESS 等 |
| `results[].directionId` | string | 方向 ID（如 employment_backend） |
| `results[].rank` | integer | 排序名次 |
| `results[].score` | number | 匹配度评分（百分制，0-100） |
| `results[].confidence` | string | HIGH / MEDIUM / LOW |
| `results[].reasons` | array | 推荐理由（各维度） |
| `results[].strengths` | array | 学生优势 |
| `results[].gaps` | array | 差距（需加强点） |
| `results[].semesterActions` | array | 学期行动建议 |
| `results[].feedback` | object | 反馈信息 |

错误：400 参数校验 / 401 未登录 / 409 业务状态不允许 / 503 智能服务不可用（已降级或可重试）。

---

#### 2. 最新推荐结果

```
GET /api/v1/students/me/recommendations/latest
```

返回最新一次推荐批次（含方向、分数、排序、可信程度、理由、优势、差距与行动建议）。

**响应 200**：同「创建推荐批次」结构；`status=SUCCESS` 时 results 已就绪。
错误：401 / 404。

---

#### 3. 推荐批次历史

```
GET /api/v1/students/me/recommendations
```

列出历史推荐批次，可关联当时的画像版本与规则版本。

**Query 参数**：`page`（页码，从 1 开始）、`size`（每页条数，最大 100）、`sort`（如 `-createdAt`）。

---

#### 4. 推荐批次详情

```
GET /api/v1/recommendation-runs/{runId}
```

按批次查看推荐结果；批次创建后为 RUNNING 时轮询此接口。

**Path 参数**：`runId`（string，必填）。

---

#### 5. 推荐反馈

```
POST /api/v1/recommendation-results/{resultId}/feedback
```

学生对单个方向的推荐结果反馈：有帮助 / 一般 / 不符合 / 不感兴趣。反馈不立即覆盖推荐结果，仅用于优化。

**Path 参数**：`resultId`（string，必填）。

**Request Body**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `feedbackType` | string | HELPFUL / 一般 / 不符合 / 不感兴趣 等 |
| `comment` | string | 反馈意见 |

---

### 画像 Profiles（学生画像模块，5 个）

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/students/me/profile/latest` | 最新画像 |
| `GET` | `/api/v1/students/me/profile/versions` | 画像版本列表 |
| `GET` | `/api/v1/profile-snapshots/{snapshotId}` | 画像快照详情 |
| `POST` | `/api/v1/students/me/profile/refresh` | 重新生成画像 |
| `POST` | `/api/v1/profile-snapshots/{snapshotId}/feedback` | 画像反馈 |

---

### 计划 Planning（目标计划模块相关，Demo 关注）

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/students/me/plans/draft` | 生成计划草案 |
| `GET` | `/api/v1/students/me/plans/latest` | 最新计划 |
| `PATCH` | `/api/v1/plans/{planId}` | 编辑计划 |
| `POST` | `/api/v1/plans/{planId}/confirm` | 确认计划 |
| `GET` | `/api/v1/plan-versions` | 计划版本历史 |
| `GET` | `/api/v1/students/me/goals` | 我的目标 |
| `POST` | `/api/v1/students/me/goals` | 设置 / 变更目标 |
| `GET` | `/api/v1/goal-versions` | 目标版本历史 |
| `GET` | `/api/v1/tasks` | 任务列表 |
| `POST` | `/api/v1/tasks` | 新增任务 |
| `PATCH` | `/api/v1/tasks/{taskId}` | 更新任务 |
| `DELETE` | `/api/v1/tasks/{taskId}` | 删除任务 |
| `POST` | `/api/v1/tasks/{taskId}/checkin` | 任务打卡 |
| `GET` | `/api/v1/students/me/reminders` | 站内提醒 |

---

## 三、通用说明

- **接口前缀**：所有接口统一以 `/api/v1` 开头。
- **鉴权**：HTTP Bearer 认证，`Authorization: Bearer <accessToken>`；另有内网头 `X-Internal-Token`（career-core 调 career-ai 用）。
- **公共请求头**：`X-Request-Id`、`Idempotency-Key`（写接口幂等）。
- **错误码**：400 参数校验 / 401 未登录或令牌失效 / 404 资源不存在 / 409 业务状态不允许 / 503 智能服务不可用。
- **文档同步**：Apifox 文档更新后需重新执行 `docs/scripts/sync_apifox_api_doc.py` 重新生成本文档；完整定义见 `docs/openapi/career-core-apis-online.md`（官方导出）与 `docs/openapi/career-core-apis-live.yaml`（OpenAPI）。
