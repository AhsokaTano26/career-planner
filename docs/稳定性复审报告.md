# 稳定性复审报告（2026-09-06 第二轮：结合业界实践）

> 方法：三路只读审查（后端/前端/AI 网关）+ 联网对照（JWT 生命周期 RFC 9700 / LiteLLM 官方网关实践）。
> 约束：不用 Redis；refresh 双写兼容一轮再切。本轮未 commit。

## P0（隐私/安全）——已修复并验证

| # | 问题 | 修复 | 验证 |
|---|---|---|---|
| 1 | 网关 `_CHAT_HISTORY` 全局内存，history 返回全局最近一条 → 跨用户串读 | 按 `studentRef` 分区（每用户 500/总用户 1000 上限）；history 必须传 `studentRef` 否则空对象 | pytest 新增 2 项（隔离/空对象）；冒烟 23 项 |
| 2 | `explain_batch` 全程未脱敏，`ai_call_log` 存明文 PII | prompt 先过 `desensitize` | pytest 49 过 |
| 3 | 缓存键只 hash prompt，换模型命中旧答案；命中复用同一 request_id 跨用户归因污染 | 键绑定模型组/promptVersion/temperature；命中每次新 request_id | pytest 过 |
| 4 | PDF 拉取 `ALLOW_PRIVATE=1` 跳过全部检查；DNS 检查与下载分两次解析（TOCTOU）；fileUrl 无校验；重定向一律拒绝 | fileUrl 加 Pydantic 校验（scheme/host/userinfo/端口）；重定向手动跟随 ≤3 跳逐跳复验；旁路开启记 warning；残留 TOCTOU 注记/IP 直连替换点 | pytest 过 |
| 5 | refresh 明文入库按值查，拖库即冒用 | SHA-256 入库 + 轮换 + 复用即吊销全用户 refresh；双写兼容（新行明文+哈希并存，读优先哈希、回退回填）；jwt.secret 启动长度校验 | 实测：轮换 200/复用 401/连带吊销 401/重登 200 |
| 6 | `/metrics` 无鉴权暴露量级数据 | 同网关 Bearer 鉴权；scene 基数上限 200（超限归 overflow） | 冒烟新增无 key 401 项 |

## P1（可靠性/正确性）——已修复并验证

| # | 问题 | 修复 |
|---|---|---|
| 7 | 复盘草稿先查后写并发重复 DRAFT | `UK(student_id, cycle)` + 冲突回读更新（幂等）；schema 与迁移同步 |
| 8 | submit 非事务状态撕裂 | `persistReview` 条件写（仅 DRAFT/SUBMITTED），0 行转 409 |
| 9 | 计划并发编辑静默覆盖 | `selectLatestPlanByStatusForUpdate`（FOR UPDATE），update/confirm 加锁读 |
| 10 | 提醒每次生成无去重 | 未读同类型同标识跳过 |
| 11 | 核心写链路无幂等（任务/打卡双击） | 可选 `Idempotency-Key`（缺省保持兼容）；controller/iface/impl 三层 |
| 12 | 唯一冲突 500、ConstraintViolation 泄漏内部信息 | DuplicateKey/DataIntegrity → 409 通用文案；Constraint 统一文案 + warn 日志 |
| 13 | 无计划时建任务 500（plan_id 非空约束） | 前置明确 400 |
| 14 | 网关预算跨零点不清 reserved（次日误 429 永不恢复） | rollover 同清；outstanding() 排障口；无 Redis 下内存语义注记 |
| 15 | fallback 判定只比 fallback 名单（上游改名漏记/误记） | 先排除主组命中，再判 fallback 名单（对照官方 Router 语义） |
| 16 | 输入无总上限（1.5M）、comment 无上限、month/estimatedHours 无约束 | messages 总 120k 封顶；comment 2000；month 正则 + hours 范围 |
| 17 | 迁移与 sql.init 赛跑（空库/双实例） | 表不存在跳过（新库由全量脚本建）；残留双实例竞态注记（正式换 Flyway） |
| 18 | Filter 每请求两次查库 | 用户行 60s 短缓存（黑名单仍实时）；版本 mismatch 回源自愈；三处版本递增同步驱逐 |
| 19 | 前端强制改密无中央拦截 | 路由守卫（改密页/ADMIN 豁免） |
| 20 | 401 重试复用已消耗 signal；download/postRaw 零超时 | 重试传原始 init；fetchWithTimeout（下载/网关 120s） |
| 21 | 学生工作台 Promise.all 任一弱依赖抖动踢 /error | 弱依赖 catch 降级（档案仍抛） |
| 22 | 分页越界“有总数无数据” | resolvePage 钳制 + 末页重拉 + vitest 4 项 |
| 23 | 雷达动态 import 无降级 | catch 切数字表兜底 |
| 24 | litellm 只钉版本无哈希 | requirements 记录核验流程 + `--require-hashes` 替换点 |

## 本轮修出的真 bug（回归中新发现）

- refresh 复用吊销与 `throw` 同事务被回滚 → `REQUIRES_NEW` 独立提交（实测连带吊销 401）。
- `CareerCoreApplicationTests` 失败：09-05 会话移走 MariaDB 方言后测试基线未同步（与本次改动无关，属历史遗留）→ 基线补等价标准 DDL + 修正过期注释。

## 验证矩阵

- 后端：`mvnw test` 106 项全过；`package` 成功
- 网关：pytest 49 项全过
- 主冒烟：10 项 ALL PASS；AI 冒烟真模式 23 项 ALL PASS（含新增 metrics 401）
- 前端：`vue-tsc` + `vite build` + `vitest` 27 项全过
- 负例实测：refresh 轮换/复用/连带吊销、幂等同键同 ID、越权 403（上一轮）

## 残留风险（后续迭代）

1. refresh 双写第二轮：停写 token 明文、删 `findByToken` 回退分支（本轮已埋注释标记）。
2. 多实例：限流/预算/缓存/history 仍进程内存（按要求不用 Redis）。
3. SSRF TOCTOU 残留：解析到 IP 后直连 + Host/SNI 固定。
4. 迁移正式换 Flyway；E2E 覆盖核心链路。
