# 复制本文件为 application-local.yml 并填入真实值(application-local.yml 已 gitignore)
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/career_core?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: career
    password: CHANGE_ME
    driver-class-name: com.mysql.cj.jdbc.Driver

jwt:
  secret: CHANGE_ME_AT_LEAST_32_BYTES_RANDOM
  access-token-ttl-seconds: 7200
  refresh-token-ttl-seconds: 604800
