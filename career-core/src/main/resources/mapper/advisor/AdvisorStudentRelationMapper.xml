<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rickgao.careercore.modules.advisor.mapper.AdvisorStudentRelationMapper">

    <select id="countByAdvisorAndStudent" resultType="int">
        SELECT COUNT(*)
        FROM advisor_student_relation
        WHERE advisor_id = #{advisorId}
          AND student_id = #{studentId}
          AND deleted = 0
    </select>

    <select id="findStudentIdsByAdvisor" resultType="string">
        SELECT student_id
        FROM advisor_student_relation
        WHERE advisor_id = #{advisorId}
          AND deleted = 0
    </select>

</mapper>
