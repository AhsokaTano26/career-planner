<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rickgao.careercore.modules.direction.mapper.FavoriteMapper">

    <resultMap id="FavoriteMap" type="com.rickgao.careercore.modules.direction.entity.Favorite">
        <id property="id" column="id"/>
        <result property="studentId" column="student_id"/>
        <result property="directionId" column="direction_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByStudent" resultMap="FavoriteMap">
        SELECT id, student_id, direction_id, created_at
        FROM favorite
        WHERE student_id = #{studentId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByStudentAndDirection" resultMap="FavoriteMap">
        SELECT id, student_id, direction_id, created_at
        FROM favorite
        WHERE student_id = #{studentId}
          AND direction_id = #{directionId}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.rickgao.careercore.modules.direction.entity.Favorite">
        INSERT INTO favorite (id, student_id, direction_id)
        VALUES (#{id}, #{studentId}, #{directionId})
    </insert>

    <delete id="deleteByStudentAndDirection">
        DELETE FROM favorite
        WHERE student_id = #{studentId} AND direction_id = #{directionId}
    </delete>

</mapper>
