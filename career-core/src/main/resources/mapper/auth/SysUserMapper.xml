<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rickgao.careercore.modules.auth.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="com.rickgao.careercore.modules.auth.entity.SysUser">
        <id property="id" column="id"/>
        <result property="studentNo" column="student_no"/>
        <result property="username" column="username"/>
        <result property="name" column="name"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="grade" column="grade"/>
        <result property="majorCategory" column="major_category"/>
        <result property="className" column="class_name"/>
        <result property="consentAgreed" column="consent_agreed"/>
        <result property="passwordChangeRequired" column="password_change_required"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <sql id="baseColumns">
        id, student_no, username, name, password_hash, role, status, grade, major_category,
        class_name, consent_agreed, password_change_required, last_login_at, created_at, updated_at, deleted
    </sql>

    <select id="findById" resultMap="SysUserMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_user
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="findByUsername" resultMap="SysUserMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="findByStudentNo" resultMap="SysUserMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_user
        WHERE student_no = #{studentNo} AND deleted = 0
    </select>

    <insert id="insert" parameterType="com.rickgao.careercore.modules.auth.entity.SysUser">
        INSERT INTO sys_user (id, student_no, username, name, password_hash, role, status, grade,
                              major_category, class_name, consent_agreed, password_change_required, last_login_at, created_at, updated_at, deleted)
        VALUES (#{id}, #{studentNo}, #{username}, #{name}, #{passwordHash}, #{role}, #{status}, #{grade},
                #{majorCategory}, #{className}, #{consentAgreed}, #{passwordChangeRequired}, #{lastLoginAt}, NOW(), NOW(), 0)
    </insert>

    <update id="updateBasic" parameterType="com.rickgao.careercore.modules.auth.entity.SysUser">
        UPDATE sys_user
        SET name = #{name},
            grade = #{grade},
            major_category = #{majorCategory},
            class_name = #{className},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateOwnName">
        UPDATE sys_user
        SET name = #{name}, updated_at = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updatePassword">
        UPDATE sys_user
        SET password_hash = #{passwordHash}, password_change_required = #{passwordChangeRequired}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateConsentAgreed">
        UPDATE sys_user
        SET consent_agreed = #{consentAgreed}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateLastLoginAt">
        UPDATE sys_user
        SET last_login_at = #{lastLoginAt}, updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
