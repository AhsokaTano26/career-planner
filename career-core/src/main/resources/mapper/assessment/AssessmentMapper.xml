<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rickgao.careercore.modules.assessment.mapper.AssessmentMapper">

    <resultMap id="QuestionnaireMap" type="com.rickgao.careercore.modules.assessment.entity.Questionnaire">
        <id property="id" column="id"/>
        <result property="type" column="type"/>
        <result property="name" column="name"/>
        <result property="typeName" column="type_name"/>
        <result property="icon" column="icon"/>
        <result property="status" column="status"/>
        <result property="version" column="version"/>
        <result property="minutes" column="minutes"/>
        <result property="tip" column="tip"/>
        <result property="publishedAt" column="published_at"/>
        <result property="publishedBy" column="published_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <resultMap id="VersionMap" type="com.rickgao.careercore.modules.assessment.entity.QuestionnaireVersion">
        <id property="id" column="id"/>
        <result property="questionnaireId" column="questionnaire_id"/>
        <result property="version" column="version"/>
        <result property="status" column="status"/>
        <result property="questionCount" column="question_count"/>
        <result property="changeNote" column="change_note"/>
        <result property="publishedAt" column="published_at"/>
        <result property="publishedBy" column="published_by"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <resultMap id="QuestionMap" type="com.rickgao.careercore.modules.assessment.entity.Question">
        <id property="id" column="id"/>
        <result property="questionnaireVersionId" column="questionnaire_version_id"/>
        <result property="text" column="text"/>
        <result property="type" column="type"/>
        <result property="dim" column="dim"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <resultMap id="OptionMap" type="com.rickgao.careercore.modules.assessment.entity.QuestionOption">
        <id property="id" column="id"/>
        <result property="questionId" column="question_id"/>
        <result property="text" column="text"/>
        <result property="scoresJson" column="scores_json"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <resultMap id="SessionMap" type="com.rickgao.careercore.modules.assessment.entity.AssessmentSession">
        <id property="id" column="id"/>
        <result property="studentId" column="student_id"/>
        <result property="questionnaireVersionId" column="questionnaire_version_id"/>
        <result property="status" column="status"/>
        <result property="totalQuestions" column="total_questions"/>
        <result property="answeredQuestions" column="answered_questions"/>
        <result property="startedAt" column="started_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="finishedAt" column="finished_at"/>
        <result property="scoreJson" column="score_json"/>
        <result property="requestId" column="request_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="listPublishedQuestionnaires" resultMap="QuestionnaireMap">
        SELECT id, type, name, type_name, icon, status, version, minutes, tip, published_at, published_by, created_at, updated_at
        FROM questionnaire
        WHERE status = 'PUBLISHED'
        ORDER BY created_at ASC
    </select>

    <select id="findQuestionnaireById" resultMap="QuestionnaireMap">
        SELECT id, type, name, type_name, icon, status, version, minutes, tip, published_at, published_by, created_at, updated_at
        FROM questionnaire
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findLatestVersion" resultMap="VersionMap">
        SELECT id, questionnaire_id, version, status, question_count, change_note, published_at, published_by, created_at
        FROM questionnaire_version
        WHERE questionnaire_id = #{questionnaireId}
        ORDER BY version DESC
        LIMIT 1
    </select>

    <select id="findVersionById" resultMap="VersionMap">
        SELECT id, questionnaire_id, version, status, question_count, change_note, published_at, published_by, created_at
        FROM questionnaire_version
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="listVersions" resultMap="VersionMap">
        SELECT id, questionnaire_id, version, status, question_count, change_note, published_at, published_by, created_at
        FROM questionnaire_version
        WHERE questionnaire_id = #{questionnaireId}
        ORDER BY version DESC
    </select>

    <select id="listQuestions" resultMap="QuestionMap">
        SELECT id, questionnaire_version_id, text, type, dim, sort_order, created_at
        FROM question
        WHERE questionnaire_version_id = #{versionId}
        ORDER BY sort_order ASC, created_at ASC
    </select>

    <select id="listOptions" resultMap="OptionMap">
        SELECT id, question_id, text, scores_json, sort_order, created_at
        FROM question_option
        WHERE question_id IN
        <foreach collection="questionIds" item="qid" open="(" separator="," close=")">
            #{qid}
        </foreach>
        ORDER BY question_id ASC, sort_order ASC
    </select>

    <insert id="insertSession" parameterType="com.rickgao.careercore.modules.assessment.entity.AssessmentSession">
        INSERT INTO assessment_session (id, student_id, questionnaire_version_id, status, total_questions, answered_questions,
                                        started_at, finished_at, score_json, request_id)
        VALUES (#{id}, #{studentId}, #{questionnaireVersionId}, #{status}, #{totalQuestions}, #{answeredQuestions},
                #{startedAt}, #{finishedAt}, #{scoreJson}, #{requestId})
    </insert>

    <select id="findSessionById" resultMap="SessionMap">
        SELECT id, student_id, questionnaire_version_id, status, total_questions, answered_questions,
               started_at, updated_at, finished_at, score_json, request_id, created_at
        FROM assessment_session
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findActiveSession" resultMap="SessionMap">
        SELECT id, student_id, questionnaire_version_id, status, total_questions, answered_questions,
               started_at, updated_at, finished_at, score_json, request_id, created_at
        FROM assessment_session
        WHERE student_id = #{studentId}
          AND status IN ('IN_PROGRESS')
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="findLatestScoredByStudent" resultMap="SessionMap">
        SELECT id, student_id, questionnaire_version_id, status, total_questions, answered_questions,
               started_at, updated_at, finished_at, score_json, request_id, created_at
        FROM assessment_session
        WHERE student_id = #{studentId}
          AND status = 'SCORED'
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="listSessions" resultMap="SessionMap">
        SELECT id, student_id, questionnaire_version_id, status, total_questions, answered_questions,
               started_at, updated_at, finished_at, score_json, request_id, created_at
        FROM assessment_session
        WHERE student_id = #{studentId}
        ORDER BY created_at DESC
    </select>

    <update id="updateSessionAnswers">
        UPDATE assessment_session
        SET answered_questions = #{answered}, status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateSessionScore">
        UPDATE assessment_session
        SET status = #{status}, score_json = #{scoreJson}, finished_at = #{finishedAt}, updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
